## User Request:
**Intent**: {{ intent }}
{% if molecule %}**Target Molecule**: {{ molecule }}{% endif %}
{% if preferences %}**Preferences**: {{ preferences }}{% endif %}

## Workspace Files:
{% if workspace_files %}
The following files are available in the workspace — reference them in `constraints.geometry_file` if relevant:
{% for f in workspace_files %}
- `{{ f.name }}` ({{ f.size_bytes }} bytes)
{% endfor %}
{% else %}
No files in workspace. If geometry is needed, use `constraints: {inline_geometry: true}` and note it in planner_notes.
{% endif %}

## Available Semantic Types (prefer these when they fit):
{% if node_summaries %}
{% set seen_types = [] %}
{% for node in node_summaries %}
{% if node.semantic_type and node.semantic_type not in seen_types %}
{% set _ = seen_types.append(node.semantic_type) %}
- **{{ node.semantic_type }}**
{% endif %}
{% endfor %}
{% else %}
No existing implementations found — propose appropriate semantic type strings.
{% endif %}

## Task:
Design a **semantic** workflow that faithfully represents the user's intent.

Key rules:
- Respect the user's explicit choices (e.g., if they request a coarse-then-precise strategy, represent it as-is)
- `constraints` must ONLY contain workspace references (`geometry_file` or `inline_geometry: true`) — no method, basis, charge, or multiplicity
- Do NOT reference specific software, port names, or file paths (except workspace filenames)
- Be concise — only add steps that are necessary to achieve the stated goal
