## Ephemeral Node Generation Request:

**Description**: {{ request.description }}

{% if request.ports %}
### Port Declarations:
- **Input ports**: {{ request.ports.get('inputs', 0) }} (auto-named I1, I2, ...)
- **Output ports**: {{ request.ports.get('outputs', 0) }} (auto-named O1, O2, ...)
{% endif %}

{% if request.context %}
{% if request.context.get('upstream') %}
### Upstream connections:
{% for conn in request.context.upstream %}
- `{{ conn.port }}` ← `{{ conn.source }}` ({{ conn.description }})
{% endfor %}
{% endif %}
{% if request.context.get('downstream') %}
### Downstream connections:
{% for conn in request.context.downstream %}
- `{{ conn.port }}` → `{{ conn.target }}` ({{ conn.description }})
{% endfor %}
{% endif %}
{% if request.context.get('onboard_params') %}
### User-provided parameters:
{% for k, v in request.context.onboard_params.items() %}
- `{{ k }}`: {{ v }}
{% endfor %}
{% endif %}
{% if request.context.get('sweep_context') %}
### Sweep Context:
This node receives aggregated output from a sweep node.
- Sweep parameter: `{{ request.context.sweep_context.sweep_param }}`
- Sweep values: {{ request.context.sweep_context.sweep_values }}
- Input I1 is a JSON array (one element per sweep iteration).
- `_sweep_keys` env var contains the original sweep values as a JSON array.
- Use `json.loads()` to parse both. **Always convert with `float()` before math/plotting.**
{% endif %}
{% endif %}

{% if input_data %}
### Real Input Data Preview (first 500 chars of each port):
{% for port_name, content in input_data.items() %}
**{{ port_name }}**:
```
{{ content[:500] }}{% if content|length > 500 %}
... (truncated){% endif %}
```
{% endfor %}
{% endif %}

{% if has_execution_history %}
### Previous Attempt (iteration {{ iteration }}):
**Script**:
```python
{{ prev_script }}
```
**Stderr**:
```
{{ prev_stderr[:2000] }}
```
{% endif %}

{% if vision_feedback %}
### Vision Evaluator Feedback:
{% for fb in vision_feedback %}
- {{ fb }}
{% endfor %}
{% endif %}

Write the script and call `sandbox_execute` to test it. When it runs successfully (return_code=0), stop.
