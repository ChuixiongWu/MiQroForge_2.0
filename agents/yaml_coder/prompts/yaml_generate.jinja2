## Semantic Workflow to Implement:
```json
{{ semantic_workflow_json }}
```

> **Note on `planner_notes`**: The planner_notes field above contains high-level scientific rationale only.
> It has NO knowledge of specific port names, file formats, or software internals.
> **Never use planner_notes to determine port connections.**
> Port connections MUST be derived solely from get_node_details() tool results.

## Suggested Nodes per Step (names only — use get_node_details for full spec):
{% for res in resolutions %}
- Step **{{ res.step_id }}**: {{ res.resolved_node or '(none — use search_nodes_by_semantic_type to find one)' }}
  {% if res.onboard_params %}Constraints hint: {{ res.onboard_params }}{% endif %}
{% endfor %}

## User Parameters:
{% if user_params %}
{% for key, val in user_params.items() %}
- {{ key }}: {{ val }}
{% endfor %}
{% else %}
(none provided — use sensible defaults for the molecule)
{% endif %}

{% if iteration > 0 and prev_yaml %}
## Previous YAML (failed validation):
```yaml
{{ prev_yaml }}
```

## Validation Errors to Fix:
{% for err in validation_errors %}
- {{ err }}
{% endfor %}

{% if evaluation_issues %}
## Evaluation Issues to Fix:
{% for issue in evaluation_issues %}
- {{ issue }}
{% endfor %}
{% endif %}
{% endif %}

**Important**:
- Before generating the YAML, call get_node_details() for each node to verify exact
  port names and onboard_params. Do NOT guess port names.
- **Use the semantic step ID (step-1, step-2, …) as each node's instance `id`.**
  This is required for frontend canvas mapping. See the example in the system prompt.

Generate the complete MF YAML for this workflow:
